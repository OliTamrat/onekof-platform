# OAuth providers

Google, GitHub, Microsoft and Apple, each configured with API
credentials from that provider — there is no OAuth without registering
the application per provider first. Full setup checklist:
`security/OAUTH_SETUP.md`. `client_id is required` is the standard
symptom of a provider not yet configured, not a bug.
