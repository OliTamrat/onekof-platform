# Cloudflare DNS — multi-tenant wildcard

`onekof.com`, `www.onekof.com` and the `*.onekof.com` wildcard (for
per-tenant subdomains) are configured in Vercel and Cloudflare — status
and the exact records: `deployment/CLOUDFLARE_DNS_SETUP.md`.
